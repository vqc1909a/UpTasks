include partials/mixins
<!DOCTYPE html>
html(lang="en")
     head
         include partials/headView
     body
          +header("Aplicación de Tareas")
          section.restablecer
               .container
                    .row.justify-content-center.align-items-center
                         .col-10.col-md-8.col-lg-6
                              form(action="", method="POST").card-body.bg-light
                                   if error
                                        .alert.alert-danger= error
                                   p.lead.text-center.text-white.mb-4.text-uppercase Coloca tu Nuevo Password
                                   //- .form-group.d-flex.align-items-center.justify-content-center
                                   //-      .col-3
                                   //-           label(for="password_actual") Password Actual:
                                   //-      .col-9
                                   //-           input(type="password" name="password_actual" placeholder="Escribe tu password actual" class="form-control", value=body && body.password_actual)

                                   .form-group.d-flex.align-items-center.justify-content-center
                                        .col-3
                                             label(for="password_nuevo") Nuevo Password:
                                        .col-9
                                             input(type="password" name="password_nuevo" placeholder="Escribe tu nuevo password" class="form-control", value=body && body.password_nuevo)
                                   
                                   .form-group.d-flex.align-items-center.justify-content-center
                                        .col-3
                                             label(for="confirm") Confirmar Nuevo Password:
                                        .col-9
                                             input(type="password" name="confirm" placeholder="Confirme tu nuevo password" class="form-control")
                                   
                                   button(type="submit").btn.btn-lg.btn-primary.btn-block Cambiar Password
